let customModule = require('../readLocalInput');

const INPUT_FILE = customModule.textToArray(customModule.getInputFile());

const DUMMY_TEST = ['+14','1','-12'];

function stringToInt(DUMMY_TEST) {
    return parseInt(DUMMY_TEST);
}

